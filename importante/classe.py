class Cachorro():
    def __init__(Self, nome, raca, dono):
        Self.nome = nome
        Self.raca = raca
        Self.dono = dono

    def texto():
        print(f"O {Self.dono} tem um cachorro da raça {Self.raca} com o nome {Self.nome} ")

primeiro = Cachorro("dog", "Pinscher", "Gustavo")
print(primeiro)
texto(primeiro)
